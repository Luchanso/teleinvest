
const watchers = {};

/**
 * Check symbol sell price is greate of value
 * @param {String} symbol Symbol of stock
 * @param {Number} value Alarm sell value
 */
export const checkStock = (symbol, value) => { };

export const asyncWatch = (id, symbol, value) => { };

export const stopAsyncWatch = (id) => { };

export const test = id => id;

